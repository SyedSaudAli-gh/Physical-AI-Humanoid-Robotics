// Constants for API endpoints and configuration
// Use relative path to leverage Docusaurus proxy configuration
const API_BASE_URL = '';

const API_ENDPOINTS = {
  CHAT: '/api/chat',  // Using relative path for proxy
};

export { API_BASE_URL, API_ENDPOINTS };